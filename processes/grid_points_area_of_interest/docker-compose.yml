services:
  app:
    container_name: create_grid_points
    build: .
    env_file:
      - .env
    # networks:
    #   - thno
    volumes:
      # - ../../shared:/usr/src/app/shared
      - clbb-modules-cache:/usr/src/app/shared

volumes:
  clbb-modules-cache:
    external: true

# networks:
#   thno:
#     external: true