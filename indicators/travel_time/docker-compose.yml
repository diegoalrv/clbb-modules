services:
  app:
    container_name: bus_travel_time
    build: .
    env_file:
      - .env
    networks:
      - thno
    volumes:
      # - assets:/usr/src/app/assets
      - clbb-modules-cache:/usr/src/app/shared

volumes:
  clbb-modules-cache:
    external: true

networks:
  thno:
    external: true
