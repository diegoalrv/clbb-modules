services:
  app:
    container_name: proximity_bus_stops
    build: .
    env_file:
      - .env
    networks:
      - thno
    volumes:
      - ../../shared:/usr/src/app/shared

networks:
  thno:
    external: true