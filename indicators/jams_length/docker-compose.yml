services:
  app:
    container_name: jams_length
    build: .
    env_file:
      - .env
    networks:
      - thno
    volumes:
      # - ../../shared:/usr/src/app/shared
      - clbb-modules-cache:/usr/src/app/shared

volumes:
  clbb-modules-cache:
    external: true

networks:
  thno:
    external: true
